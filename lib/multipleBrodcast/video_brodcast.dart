import 'package:flutter/material.dart';

class VideoBroadcastScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Video Broadcast Screen", style: TextStyle(fontSize: 18)),
    );
  }
}
