

import 'package:flutter/material.dart';

class AudioBroadcastScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Audio Broadcast Screen", style: TextStyle(fontSize: 18)),
    );
  }
}
